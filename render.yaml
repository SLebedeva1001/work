services:
  - type: web
    name: supplier-kanban
    runtime: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: ADMIN_EMAIL
        value: s.vento85@gmail.com
      - key: APP_URL
        value: https://work-uw04.onrender.com
      - key: DATA_FILE
        value: /var/data/board.json
    disk:
      name: supplier-board-data
      mountPath: /var/data
      sizeGB: 1
